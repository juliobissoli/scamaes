import { NextPage } from "next";

const Collaborator: NextPage = (props) => {
  // console.log("==> ", props);
  // const router = useRouter();
  return <h1 className="text-4xl">Collaborator</h1>;
};

export default Collaborator;
